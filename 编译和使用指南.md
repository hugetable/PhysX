# PhysX 生态系统完整编译和使用指南

## 目录

1. [系统要求](#1-系统要求)
2. [PhysX 核心库编译](#2-physx-核心库编译)
3. [BLAST 破坏系统编译](#3-blast-破坏系统编译)
4. [FLOW 流体系统编译](#4-flow-流体系统编译)
5. [OMNI Omniverse集成编译](#5-omni-omniverse集成编译)
6. [依赖关系总结](#6-依赖关系总结)
7. [常见问题排查](#7-常见问题排查)

---

## 1. 系统要求

### 1.1 操作系统

| 平台 | PhysX | BLAST | FLOW | OMNI |
|------|-------|-------|------|------|
| **Linux** | ✅ Ubuntu 18.04+ | ✅ | ✅ | ✅ |
| **Windows** | ✅ Windows 10+ | ✅ | ✅ | ✅ |
| **macOS** | ⚠️ 有限支持 | ❌ | ❌ | ❌ |

### 1.2 编译工具链

#### Linux
```bash
# 必需工具
sudo apt-get update
sudo apt-get install -y \
    build-essential \
    cmake \
    git \
    git-lfs \
    python3 \
    python3-pip \
    libx11-dev \
    libxrandr-dev \
    libxcursor-dev \
    libxi-dev \
    libgl1-mesa-dev \
    libglu1-mesa-dev

# GCC 版本
gcc --version  # 需要 GCC 7.0+

# CMake 版本
cmake --version  # 需要 CMake 3.14+
```

#### Windows
```powershell
# Visual Studio 2019 或 2022
# - 桌面 C++ 开发工作负载
# - Windows 10 SDK

# CMake (推荐 3.18+)
# Git (带 Git LFS)
# Python 3.7+
```

### 1.3 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|---------|---------|
| **CPU** | Intel Core i5 / AMD Ryzen 5 | Intel Core i7 / AMD Ryzen 7 |
| **内存** | 8 GB RAM | 16 GB+ RAM |
| **GPU** | - | NVIDIA GPU (CUDA 11.0+) |
| **存储** | 20 GB 可用空间 | 50 GB+ SSD |

---

## 2. PhysX 核心库编译

### 2.1 克隆仓库

```bash
# 克隆主仓库（已完成）
cd /home/user/PhysX

# 确保 Git LFS 已初始化
git lfs install
git lfs pull
```

### 2.2 生成项目文件

#### Linux
```bash
cd physx

# 查看可用预设
./generate_projects.sh

# 选择预设（示例）
./generate_projects.sh linux-carbonite

# 可用的主要预设：
# - linux-carbonite     (标准 Linux 构建)
# - linux-aarch64-carbonite (ARM64)
# - linux-debug        (调试版本)
# - linux-release      (发布版本)
```

#### Windows
```powershell
cd physx

# 生成 Visual Studio 解决方案
./generate_projects.bat vc16win64-carbonite

# 可用预设：
# - vc16win64-carbonite  (VS 2019, 64-bit)
# - vc17win64-carbonite  (VS 2022, 64-bit)
```

### 2.3 编译

#### Linux (Make)
```bash
cd physx/compiler/linux-release

# 编译 Release 版本
make -j$(nproc)

# 编译 Debug 版本
cd ../linux-debug
make -j$(nproc)

# 编译 Checked 版本（带断言）
cd ../linux-checked
make -j$(nproc)
```

#### Windows (Visual Studio)
```powershell
# 方式 1: Visual Studio IDE
# 打开 physx/compiler/vc16win64/PhysXSDK.sln

# 方式 2: 命令行
cd physx\compiler\vc16win64
msbuild PhysXSDK.sln /p:Configuration=release /p:Platform=x64 /m
```

### 2.4 验证编译

```bash
# Linux
ls physx/bin/linux.clang/release/
# 应看到:
#   - libPhysX_64.so
#   - libPhysXCommon_64.so
#   - libPhysXFoundation_64.so
#   - libPhysXCooking_64.so

# Windows
dir physx\bin\win.x86_64.vc142.mt\release\
# 应看到:
#   - PhysX_64.dll
#   - PhysXCommon_64.dll
#   - PhysXFoundation_64.dll
#   - PhysXCooking_64.dll
```

### 2.5 安装（可选）

```bash
# Linux
sudo cp physx/bin/linux.clang/release/*.so /usr/local/lib/
sudo cp -r physx/include/* /usr/local/include/

sudo ldconfig

# Windows - 将 DLL 复制到你的项目输出目录
```

---

## 3. BLAST 破坏系统编译

### 3.1 前提条件

**BLAST 现在是独立的**，不再强制依赖 PhysX。

### 3.2 生成项目

#### Linux
```bash
cd /home/user/PhysX/blast

# 使用 repo 工具生成
./repo.sh build -c release

# 或者直接使用构建脚本
./build.sh -c release
```

#### Windows
```powershell
cd blast

# 使用 repo 工具
./repo.bat build -c release

# 或
./build.bat -c release
```

### 3.3 编译配置

**可用配置**:
- `release` - 发布版本（优化）
- `debug` - 调试版本
- `profile` - 性能分析版本

```bash
# 多配置编译
./repo.sh build -c debug
./repo.sh build -c release
./repo.sh build -c profile
```

### 3.4 输出位置

```
blast/bin/
  ├── linux-x86_64/
  │   ├── release/
  │   │   ├── libNvBlast_x64.so
  │   │   ├── libNvBlastTk_x64.so
  │   │   └── libNvBlastExtSerialization_x64.so
  │   └── debug/
  └── ...
```

### 3.5 验证

```bash
# 运行示例（如果有）
cd blast/bin/linux-x86_64/release/
./SampleApp  # (如果存在示例应用)

# 查看库依赖
ldd libNvBlast_x64.so
```

---

## 4. FLOW 流体系统编译

### 4.1 前提条件

**FLOW 完全独立**，不依赖 PhysX 或 BLAST。

**依赖**:
- Vulkan SDK (推荐) 或 CPU 后端
- CMake 3.14+

### 4.2 安装 Vulkan SDK

#### Linux
```bash
# Ubuntu/Debian
wget -qO - https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo apt-key add -
sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-focal.list https://packages.lunarg.com/vulkan/lunarg-vulkan-focal.list
sudo apt update
sudo apt install vulkan-sdk

# 验证
vulkaninfo
```

#### Windows
```powershell
# 下载 Vulkan SDK: https://vulkan.lunarg.com/
# 安装后设置环境变量 VULKAN_SDK
```

### 4.3 生成项目

#### Linux
```bash
cd /home/user/PhysX/flow

# 生成 Makefile
./generate_projects.sh linux

# ARM64
./generate_projects_aarch64.sh
```

#### Windows
```powershell
cd flow

# 生成 Visual Studio 项目
./generate_projects.bat
```

### 4.4 编译

#### Linux
```bash
cd flow

# 使用构建脚本
./build.sh -c release

# 或手动
cd _compiler/linux-x86_64/release
make -j$(nproc)
```

#### Windows
```powershell
cd flow

# 使用构建脚本
./build.bat -c release

# 或使用 msbuild
cd _compiler\windows-x86_64
msbuild NvFlow.sln /p:Configuration=Release /m
```

### 4.5 输出位置

```
flow/bin/
  ├── linux-x86_64/
  │   └── release/
  │       ├── libNvFlowVK.so      # Vulkan 后端
  │       └── libNvFlowCPU.so     # CPU 后端
  └── windows-x86_64/
      └── release/
          ├── NvFlowVK.dll
          └── NvFlowCPU.dll
```

### 4.6 测试

```bash
# 运行示例（如果提供）
cd flow/bin/linux-x86_64/release
./FlowSample
```

---

## 5. OMNI Omniverse集成编译

### 5.1 前提条件

**OMNI 强依赖 PhysX**，必须先编译 PhysX。

**额外依赖**:
- Kit SDK (通过 packman 自动下载)
- USD SDK (通过 packman 自动下载)
- Python 3.9+ (Kit 使用)

### 5.2 初始化依赖

```bash
cd /home/user/PhysX/omni

# 清理之前的依赖（首次构建）
./repo.sh source clear

# 拉取依赖
./repo.sh build -r
```

### 5.3 构建选项

#### 选项 1: 使用预编译 PhysX

```bash
# 使用 packman 下载的 PhysX 包
cd omni
./repo.sh build -r -c release
```

#### 选项 2: 使用本地 PhysX 源码 (--devphysx)

```bash
# 1. 确保 PhysX 已编译（checked 配置）
cd /home/user/PhysX/physx
./generate_projects.sh linux-carbonite
cd compiler/linux-checked
make -j$(nproc)

# 2. 构建 OMNI，链接本地 PhysX
cd /home/user/PhysX/omni
./repo.sh build --devphysx -c release
```

#### 选项 3: 本地 Schema 生成 (--devschema)

```bash
# 使用 ./build.sh (会触发 schema 生成)
cd omni
./build.sh --devschema -c release
```

### 5.4 完整构建示例

```bash
cd /home/user/PhysX

# 1. 构建 PhysX (checked 配置)
cd physx
./generate_projects.sh linux-carbonite
cd compiler/linux-checked
make -j$(nproc)
cd ../../..

# 2. 构建 OMNI
cd omni
./build.sh --devphysx --devschema -c release
```

### 5.5 输出位置

```
omni/_build/
  ├── linux-x86_64/
  │   └── release/
  │       ├── extsPhysics/
  │       │   ├── omni.physx/
  │       │   ├── omni.physx.fabric/
  │       │   ├── omni.usdphysics/
  │       │   └── ...
  │       ├── kit/
  │       │   └── kit (可执行文件)
  │       └── apps/
  │           └── omni.bloky.physx.kit
  └── ...
```

### 5.6 运行测试应用

```bash
cd omni/_build/linux-x86_64/release

# 运行 Bloky PhysX 示例应用
./apps/omni.bloky.physx.kit.sh

# 或使用 kit 直接加载
./kit/kit --portable-root . apps/omni.bloky.physx.kit
```

### 5.7 与 IsaacSim 集成

```bash
# 1. 克隆 IsaacSim
git clone https://github.com/isaac-sim/IsaacSim.git
cd IsaacSim

# 2. 链接 Omniverse PhysX 的 kit-kernel
cd ../omni
./repo.sh source link kit-kernel /path/to/IsaacSim/_build/linux-x86_64/release/kit

# 3. 重新构建 OMNI
./repo.sh build --devphysx --devschema -c release

# 4. 链接回 IsaacSim
cd ../IsaacSim
./repo.sh source link physx /path/to/PhysX/physx

# 5. 运行 IsaacSim
./isaac-sim.sh --devFolder /path/to/PhysX/omni/_build/linux-x86_64/release
```

---

## 6. 依赖关系总结

### 6.1 依赖图

```
         ┌────────────┐
         │   PhysX    │
         │  (核心库)   │
         └─────┬──────┘
               │
    ┌──────────┼──────────┐
    │          │          │
    │          │    ┌─────▼─────┐
    │          │    │   OMNI    │
    │          │    │(强依赖)    │
    │          │    └───────────┘
    │          │
 ┌──▼───┐   ┌─▼────┐
 │BLAST │   │FLOW  │
 │(独立) │   │(独立) │
 └──────┘   └──────┘

图例:
  ─▼─ : 强依赖
  独立 : 可单独编译
```

### 6.2 构建顺序

**独立构建**:
```bash
# 每个库可单独构建
physx  → 独立
blast  → 独立
flow   → 独立
omni   → 需要 PhysX
```

**完整构建顺序**:
```
1. PhysX (核心)
2. BLAST (可选，独立)
3. FLOW  (可选，独立)
4. OMNI  (依赖 PhysX)
```

---

## 7. 常见问题排查

### 7.1 PhysX 编译问题

#### 问题: 找不到 CUDA
```bash
# 错误信息
CMake Error: Could not find CUDA

# 解决方案
# 如果不需要 GPU 加速，可以禁用
./generate_projects.sh linux-carbonite --no-cuda

# 或安装 CUDA Toolkit
# https://developer.nvidia.com/cuda-downloads
```

#### 问题: GCC 版本过低
```bash
# 错误: requires GCC 7.0+

# Ubuntu 18.04 升级 GCC
sudo apt-get install gcc-9 g++-9
sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-9 90
sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-9 90
```

#### 问题: 内存不足
```bash
# 错误: virtual memory exhausted

# 减少并行任务数
make -j2  # 而不是 -j$(nproc)

# 或增加交换空间
sudo fallocate -l 8G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### 7.2 BLAST 编译问题

#### 问题: Packman 下载失败
```bash
# 错误: Failed to download dependencies

# 解决: 使用代理或镜像
export HTTP_PROXY=http://your-proxy:port
export HTTPS_PROXY=http://your-proxy:port

# 或手动下载依赖
# 编辑 blast/deps/packman.xml
```

#### 问题: 找不到 Python
```bash
# 确保 Python 3 在 PATH 中
which python3

# 设置环境变量
export PYTHON=/usr/bin/python3
```

### 7.3 FLOW 编译问题

#### 问题: Vulkan SDK 未找到
```bash
# 错误: Could not find Vulkan SDK

# Linux
export VULKAN_SDK=/path/to/vulkan/sdk
export PATH=$VULKAN_SDK/bin:$PATH
export LD_LIBRARY_PATH=$VULKAN_SDK/lib:$LD_LIBRARY_PATH

# Windows (PowerShell)
$env:VULKAN_SDK="C:\VulkanSDK\1.3.xxx.x"
```

#### 问题: 只编译 CPU 后端
```bash
# 编辑 flow/premake5.lua，禁用 Vulkan 后端
# 或使用 CPU-only 预设（如果提供）
```

### 7.4 OMNI 编译问题

#### 问题: PhysX 库未找到
```bash
# 错误: Cannot find PhysX libraries

# 确保 PhysX 使用 'checked' 配置编译
cd physx/compiler/linux-checked
make -j$(nproc)

# 或使用预编译包（移除 --devphysx）
cd omni
./repo.sh build -c release
```

#### 问题: USD Schema 生成失败
```bash
# 错误: usdGenSchema failed

# 确保使用 ./build.sh 而非 ./repo.sh build
cd omni
./build.sh --devschema -c release
```

#### 问题: Kit SDK 版本不匹配
```bash
# 错误: Incompatible Kit SDK version

# 清理并重新拉取
cd omni
./repo.sh source clear
rm -rf _build
./repo.sh build -r -c release
```

### 7.5 运行时问题

#### 问题: 共享库未找到
```bash
# Linux
# 错误: error while loading shared libraries: libPhysX_64.so

# 临时解决
export LD_LIBRARY_PATH=/path/to/physx/bin/linux.clang/release:$LD_LIBRARY_PATH

# 永久解决
sudo sh -c 'echo "/path/to/physx/bin/linux.clang/release" > /etc/ld.so.conf.d/physx.conf'
sudo ldconfig
```

#### 问题: GPU 加速不工作
```bash
# 检查 CUDA 驱动
nvidia-smi

# 检查 CUDA 运行时
nvcc --version

# PhysX GPU 诊断
# 运行示例程序并查看日志
```

---

## 8. 快速构建脚本

### 8.1 完整构建 (Linux)

创建 `build_all.sh`:

```bash
#!/bin/bash
set -e

BASE_DIR=/home/user/PhysX
NPROC=$(nproc)

echo "========== 1. 构建 PhysX =========="
cd $BASE_DIR/physx
./generate_projects.sh linux-carbonite
cd compiler/linux-checked
make -j$NPROC
cd $BASE_DIR

echo "========== 2. 构建 BLAST =========="
cd $BASE_DIR/blast
./repo.sh build -c release

echo "========== 3. 构建 FLOW =========="
cd $BASE_DIR/flow
./build.sh -c release

echo "========== 4. 构建 OMNI =========="
cd $BASE_DIR/omni
./build.sh --devphysx --devschema -c release

echo "========== 构建完成！ =========="
echo "PhysX:  $BASE_DIR/physx/bin/linux.clang/checked/"
echo "BLAST:  $BASE_DIR/blast/bin/linux-x86_64/release/"
echo "FLOW:   $BASE_DIR/flow/bin/linux-x86_64/release/"
echo "OMNI:   $BASE_DIR/omni/_build/linux-x86_64/release/"
```

### 8.2 清理脚本

创建 `clean_all.sh`:

```bash
#!/bin/bash

BASE_DIR=/home/user/PhysX

echo "清理 PhysX..."
cd $BASE_DIR/physx
rm -rf compiler/

echo "清理 BLAST..."
cd $BASE_DIR/blast
rm -rf _build/ bin/

echo "清理 FLOW..."
cd $BASE_DIR/flow
rm -rf _compiler/ bin/

echo "清理 OMNI..."
cd $BASE_DIR/omni
rm -rf _build/

echo "清理完成！"
```

---

## 9. 使用示例

### 9.1 PhysX C++ 示例

```cpp
#include <PxPhysicsAPI.h>

using namespace physx;

int main() {
    // 1. 创建 Foundation
    PxDefaultAllocator allocator;
    PxDefaultErrorCallback errorCallback;
    PxFoundation* foundation = PxCreateFoundation(
        PX_PHYSICS_VERSION, allocator, errorCallback);

    // 2. 创建 PhysX SDK
    PxPhysics* physics = PxCreatePhysics(
        PX_PHYSICS_VERSION, *foundation,
        PxTolerancesScale());

    // 3. 创建场景
    PxSceneDesc sceneDesc(physics->getTolerancesScale());
    sceneDesc.gravity = PxVec3(0.0f, -9.81f, 0.0f);
    PxScene* scene = physics->createScene(sceneDesc);

    // 4. 创建刚体
    PxRigidDynamic* sphere = PxCreateDynamic(
        *physics,
        PxTransform(PxVec3(0, 10, 0)),
        PxSphereGeometry(1.0f),
        *physics->createMaterial(0.5f, 0.5f, 0.6f),
        10.0f);
    scene->addActor(*sphere);

    // 5. 模拟
    for (int i = 0; i < 100; i++) {
        scene->simulate(1.0f / 60.0f);
        scene->fetchResults(true);

        PxVec3 pos = sphere->getGlobalPose().p;
        printf("Frame %d: y = %.2f\n", i, pos.y);
    }

    // 6. 清理
    scene->release();
    physics->release();
    foundation->release();

    return 0;
}
```

**编译**:
```bash
g++ -o physx_sample main.cpp \
    -I/home/user/PhysX/physx/include \
    -L/home/user/PhysX/physx/bin/linux.clang/release \
    -lPhysX_64 -lPhysXCommon_64 -lPhysXFoundation_64 \
    -Wl,-rpath,/home/user/PhysX/physx/bin/linux.clang/release
```

### 9.2 OMNI Python 示例

```python
from pxr import Usd, UsdGeom, UsdPhysics
import omni.physx

# 创建 Stage
stage = Usd.Stage.CreateInMemory()

# 创建物理场景
scene = UsdPhysics.Scene.Define(stage, "/physicsScene")
scene.CreateGravityMagnitudeAttr().Set(9.81)

# 创建刚体球
sphere = UsdGeom.Sphere.Define(stage, "/sphere")
sphere.CreateRadiusAttr().Set(1.0)
sphere.AddTranslateOp().Set((0, 10, 0))

UsdPhysics.RigidBodyAPI.Apply(sphere.GetPrim())
UsdPhysics.CollisionAPI.Apply(sphere.GetPrim())

# 启动模拟
omni.physx.get_physx_interface().start_simulation()
```

---

## 10. 总结

### 10.1 构建时间估计

| 库 | 配置 | 时间 (8 核 CPU) |
|---|------|----------------|
| PhysX | Checked | ~15-20 分钟 |
| BLAST | Release | ~5-10 分钟 |
| FLOW | Release | ~10-15 分钟 |
| OMNI | Release | ~20-30 分钟 |
| **总计** | | **~50-75 分钟** |

### 10.2 磁盘空间使用

```
physx/:  ~8 GB  (源码 + 编译产物)
blast/:  ~2 GB
flow/:   ~3 GB
omni/:   ~15 GB (包含 Kit SDK, USD)
───────────────
总计:    ~28 GB
```

### 10.3 推荐构建配置

**开发环境**:
```bash
PhysX: checked (带断言，便于调试)
BLAST: debug
FLOW:  debug
OMNI:  release + --devphysx + --devschema
```

**生产环境**:
```bash
PhysX: release (最高性能)
BLAST: release
FLOW:  release
OMNI:  release (使用预编译 PhysX)
```

---

**文档版本**: 1.0
**创建日期**: 2025-11-05
**适用平台**: Linux (Ubuntu 18.04+), Windows 10+
